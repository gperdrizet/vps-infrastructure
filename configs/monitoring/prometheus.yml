# Prometheus Configuration for LogKeep
global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'logkeep-production'
    environment: 'production'

# Alertmanager configuration for email alerts
alerting:
  alertmanagers:
    - static_configs:
        - targets: ['logkeep-alertmanager:9093']

# Load alert rules
rule_files:
  - '/etc/prometheus/alert-rules.yml'

# Scrape configurations
scrape_configs:
  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          service: 'prometheus'

  # Node Exporter (system metrics)
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['logkeep-node-exporter:9100']
        labels:
          service: 'node-exporter'
          instance: 'vps-primary'

  # PostgreSQL Exporter
  - job_name: 'postgres-exporter'
    static_configs:
      - targets: ['logkeep-postgres-exporter:9187']
        labels:
          service: 'postgresql'
          database: 'logkeep'

  # LogKeep Application (Blue)
  - job_name: 'logkeep-blue'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['172.18.0.1:8001']
        labels:
          service: 'logkeep'
          slot: 'blue'
          environment: 'production'

  # LogKeep Application (Green)
  - job_name: 'logkeep-green'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['172.18.0.1:8002']
        labels:
          service: 'logkeep'
          slot: 'green'
          environment: 'production'

  # LogKeep Staging
  - job_name: 'logkeep-staging'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['logkeep-staging:8000']
        labels:
          service: 'logkeep'
          slot: 'staging'
          environment: 'staging'

  # Docker Container Metrics
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['logkeep-cadvisor:8080']
        labels:
          service: 'cadvisor'
          instance: 'vps-primary'

  # Nginx Metrics
  - job_name: 'nginx'
    static_configs:
      - targets: ['logkeep-nginx-exporter:9113']
        labels:
          service: 'nginx'
          instance: 'vps-primary'

  # Headscale metrics
  - job_name: 'headscale'
    static_configs:
      - targets: ['host.docker.internal:9099']
        labels:
          service: 'headscale'
          instance: 'vps-primary'

  # SSL Certificate Monitoring
  - job_name: 'ssl-certs'
    metrics_path: /probe
    params:
      module: [http_2xx]
    static_configs:
      - targets:
          - https://headscale.perdrizet.org
          - https://bench.perdrizet.org
          - https://staging.perdrizet.org
          - https://llm.perdrizet.org
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: logkeep-blackbox-exporter:9115
